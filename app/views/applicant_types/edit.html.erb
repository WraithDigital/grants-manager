<%= form_with model: @applicant_type, url: applicant_type_path(@applicant_type), method: :patch, local: true, class: 'slim' do |f| %>
  <%= render 'form', f: f, button_text: 'Update Applicant Type' %>
<% end %>
